# Project history

## 2026-09-14 16:21 +07 — explicit known-failure merge override

The user explicitly authorizes promotion of this candidate to develop and main
despite known failures. This overrides the PASS-only merge gate, not security
boundaries, test results or VPS deployment authorization.

Latest completed full offline run: 134/134 PASS (startup completion fix).
Fresh VPS gates: 15/26 PASS; 11 unpassed: 4,5,6,7,8,12,16,18,21,22,25.
Latest case4 fails its 240-second PDF delivery deadline. Case5 remains
10,898ms against 5,000ms. Actual native-client quoted-file delivery stays open.
Later negated-attribution oracle and continuous-message observer changes have
focused unit/syntax checks only, not a fresh complete offline/live rerun.
HTML shell/guard safe-path detours and repeated PDF layout rendering remain
unresolved latency risks. A proposed submission helper was not applied.
Private evidence and operator configuration are excluded from promotion.

Latest attribution refinement offline134/134 passes, but live PDF case4 fails
an actual startup race: workflow marks a startup-buffered event idle/failed
before the replayed agent begins. Source-aware startup-buffer waiting and exact
owned cancellation fencing pass local regressions; normal deployment pending
gate23's terminal storage/cleanup run. VPS14/26 PASS,12unpassed. No promotion.

Newest fresh progress **14/26 PASS, 12 unpassed**: gate 19 passes native quoted
DM/group output, exact replies, no crossover and terminal settlement in48.89s.
Incoming contexts remain injected; real-client quoted-file delivery stays open.
File-gen attribution refinement validates and is normally deployed, with fresh
PDF/full offline reruns running. Strengthened actual-storage/cleanup note-locale
gate staged. No operator combo change or conditional promotion.

Fresh progress **13/26 PASS, 13 unpassed**: gate 14 passes source-bound terminal
policy refusal, no attachments/late extras and semantic quality 10/10. The
grounding candidate passes 134/134 offline entries. Gate 4 still fails its
unchanged 240-second limit (266s ACK) and native semantic review: WMO corrected,
but model data called measured and `is_day` used to invent sunshine. Further
skill/oracle refinement is staged; native quote concurrency rerun running.
Native-client file quote and strict latency remain open. No conditional MR.

Latest fresh VPS progress is **12/26 PASS, 14 unpassed**. Gates 17 and 13 pass
private env cleanup and real isolated OpenBao rotation/load/scrub/consumer
checks, with exact owned cleanup and unchanged operator env/KV/token/worker.
Gate 4 fails its unchanged 240-second timeout, then delivers late (~366s).
Its exact acknowledged PDF also mislabels WMO 55. Improve source-grounded
file-gen guidance and add the actual caption regression; normal deployment is
complete, fresh PDF/offline reruns pending. Earlier 134/134 offline evidence
predates this skill update. Latency/native quote delivery remain open; no MR.

Latest deployed deferred-note candidate passes 134/134 offline entries and
gate 24/26 (source-provenanced notes, requested identifier, terminal silence,
owned cleanup). Fresh VPS progress: 10/26 passed, 16 unpassed. PDF gate 4's
observer is audited to review the exact acknowledged artifact on all pages,
not arbitrary drafts or unrelated logs; its new live run remains pending.
Strict latency and native-client quote delivery remain open; no promotion yet.

Gate 15/26 passes real embeddings and isolated production-route SQL/vector
compaction, repeat grounding and owned cleanup. Fresh VPS progress is 9/26
passed, 17 unpassed. The memory fix passes a complete 134/134 offline index.
A subsequent deferred-note source/provenance/scope fix and validated notes
prompt update are normally deployed; their full offline and silent-background
reruns are running. Gate 24 previously failed omitted user content, not proof
that no notes were saved. Only its four proven owned fixtures were backed up
and soft-deleted. Latency and native-client quote delivery remain open.

Latest gate 20/26 passes exact-source DM/group delivery, semantic review (9/10)
and native visual review of both acknowledged DOCX files. Eight VPS gates are
freshly passed; 18 remain unpassed. Gate 5/26 fails the unchanged five-second
SLO (10,898 ms). Silent-background gate 24/26 is running. A new reproduced
Memory Worker vector-identity/false-compact-success fix passes its focused
regression but awaits normal deployment and fresh offline/live verification.
Earlier 134/134 evidence predates this latest memory source change.

The newest normally deployed source passes 134/134 offline index entries.
Both concurrent DOCX files now acknowledge their original channel/source, but
the live case awaits content-bound delivered-draft identification and final
semantic/visual review. Latency and silent-background oracles are strengthened
before their pending live gates; no incomplete semantic check counts as PASS.

Conversion DTO/callback source loss is reproduced and fixed; four HTTP-boundary
regressions pass. The validated skill documents exact original-source messaging
delivery and destination-free standalone behavior. Fresh live verification is
still required; native quote delivery is not certified passed.

The subsequent clean concurrency gate fails on a silent private DM draft and
instruction-as-content rendering. Exact shortcut path handling and model-owned
natural document authoring are fixed in source; the newest candidate requires
another complete offline/live rerun. The older 134/134 result is historical.

The latest candidate passes all 134 offline index entries after terminal
antivirus and text-only queued-event compatibility fixes. Fresh VPS gate 9/26
passes five security checks; gate 3/26 passes an actual original-source greeting
ACK. Seven indexed VPS gates have fresh PASS evidence, with 19 still unpassed.
Native quoted-file conversion renders all three pages correctly but its Zalo
delivery failed; clean DM/group concurrency is being rerun. See the dated audit.

Latest main-deployment verification found a directly queued document receipt
false negative, then a group artifact sent with DM type and no source. Durable
fixes and failing-before-fix regressions are recorded in the dated production
audit. Both fixes have been normally deployed and the complete offline index
passes; remaining fresh live gates are required. The earlier override does not apply.

The later operator explicitly authorizes promotion of this improvement set
despite the incomplete live gate. See the dated production audit and changelog;
the recorded failure and outstanding tests remain unchanged.

Current worker/feature descriptions and fresh activation defaults are indexed
in [the architecture-review catalog](./07-worker-feature-catalog.md). The
continued lab activated required antivirus after verified backup; native
PDF/PPTX image conversions pass. See the dated audit for remaining release gates.
The later offline index passes 134/134 and both live note exclusions pass.
The PDF's single page passes native-text and visual review. Late completion and
classifier scope expansion initially failed the PPTX gate; their core fixes and
required reruns are recorded in the audit. A later PDF run exposed pre-ACK
private-review chatter; typed result-only delivery now defers that text and
fences timed-out background jobs before releasing their claims. Fresh live
capacity and scanner checks pass 3/3 and 6/6. The subsequent PDF rerun delivers
one requested file without chatter; its actual single page passes visual and
native-language inspection. The fresh PPTX also delivers one file only; all three
native preview pages pass visual, language and current-request scope review.
Both selected artifacts match the delivered byte checksums. Expanded chat
conversions and the broader release matrix remain separate gates.
The subsequent eight conversational conversion cases pass, including quoted
PPTX-to-PDF/images and all-sheet workbook-to-DOCX. Native quote bubbles are not
established by synthetic incoming request IDs.
Standalone notes pass 4/4 with messaging unreachable, and a real direct
scheduled note passes 2/2. Monitoring passes with current Compose service labels;
its gate now rejects missing/invalid/decimal-zero gauges. Typed document jobs
bypass shared-directory discovery; eight focused regressions and the latest
134/134 offline rerun pass on the deployed source. No promotion is implied.

This file is the stable navigation point for project history.

- Root-cause records are maintained by date under [`history/`](../history/README.md).
- Operational changes and incident pointers are indexed in [`scripts/HISTORY.md`](../scripts/HISTORY.md).
- Release-facing changes are recorded in [`docs/CHANGELOG.md`](./CHANGELOG.md).

History entries must describe reusable symptoms, causes, decisions, fixes,
verification, and prevention. Do not copy chat messages, credentials, host
identities, or ticket-specific wording into documentation.

## 2026-09-14 — scoped records, standalone execution, and visual documents

See [current fixes and open release gates](../history/2026-09-14/README.md).
The candidate also adds file conversion with explicit fidelity modes and native
asynchronous schedule admission. Real-engine tests do not replace chat gates.
Quoted conversion sources have explicit routing/source-content regressions;
formatted authoring requires private preview before selected-file delivery.
The memory-manager source component is now memory-worker; historical incident
names remain accurate. Live document failures are retained as failures until
fresh terminal-delivery and visual gates pass.

See [production delivery audit](../history/2026-09-14/production-gap-audit.md)
for five reproduced core invariants and the separately verified disabled-scan
deployment gap. These remain release blockers, not completed fixes.

## 2026-09-13 — quiet background notes and typography candidate

See [root-cause and pending live gates](../history/2026-09-13/README.md).
The operator explicitly accepted a one-time merge-gate override for develop
and main; failed live image quality evidence remains unchanged in that record.

## 2026-09-15 — safe note mutations and composed-image delivery

See `history/2026-09-15/README.md`. Bulk destructive note mutations now preview
the exact matched records and require an explicit confirmation before any
delete/update, and delete/update report their own outcome instead of the save
line. The Zalo plugin now vendors `artifact_delivery.py`, restoring composed
image delivery that failed with `ModuleNotFoundError` because the read-only
plugin source mount shadowed the runtime copy.

## 2026-09-12 — one-pass grounded image composition

See `history/2026-09-12/README.md` for removal of the Pillow information-card
renderer after it created gray canvas bands and obscured scene content. The same
record covers native web-provider registration plus deployment activation and
reload, exact DM/group session isolation, quiet async workflow delivery, and
empty active-queue repair, quiet async workflow delivery, and human-readable
security alerts. It also records two release-gate findings: live file watching
leaked private document-build sidecars, and dimension-only image acceptance
delivered visibly defective compositions. Typed-output watcher deferral and
vision-scored combo failover close those boundaries.

## 2026-09-09 — scheduled composed-image execution

See `history/2026-09-09/README.md` for the queue-enabled schedule-fire gap that
discarded a valid persisted search→image graph, quoted an internal schedule id,
and omitted task-local attachment correlation. The same correction adds
same-language note UX, removes an unnecessary composed-plan environment knob,
and makes the full scheduled-image fire a mandatory release case. The live
DM/group concurrency gate also found that explicit quoted text was combined
with an unrelated completed session; quote context now takes precedence.
Follow-up gates preserved verbatim atomic prompts through the queue, repaired
dispatcher attachment correlation and scheduled environment loading, and made
shared bottom/left information regions independent of subject count. The same
record documents the retired six-line payload cap and the live harness change
from optional bridge echoes to durable acknowledged delivery evidence. A later
visual review found that the renderer still forced a model-selected bottom band
to full image width; bands are now content-sized and bounded so they preserve
the unrequested side of the scene. The record also covers stale attachment
recall in explicit replies, same-turn routed-search enforcement, and removal of
test-created schedule leakage across live cases.
The uninterrupted clean-deploy run then exposed a broader attachment-recall
boundary: old extracts were appended to every later text-only request, causing
new URL, archive, and DOCX turns to take unrelated long paths and block their
conversation queue. Recall is now conservative and explicit; the archive and
post-restart gates also wait for real terminal/bridge readiness evidence.
The resumed 131-case release gate exposed a cross-process autosend gap:
Dispatcher delivered the requested PDF and claimed it in the shared session,
then the adapter skipped that claim and fell through to an older embedded hero
image. A claimed rich document now terminates that turn's older-file scan, and
the live oracle checks acknowledged source-correlated image delivery directly.
The focused rerun then exposed a startup-order regression: Hermes' later
image-level bundled-skill sync installed a generic PDF skill even after the
entrypoint cleaned categorized clones, sending chat creation back to local
FPDF scripts. Replica startup now retains the restrictive repository wrappers,
suppresses the bundled Office skill names before that later sync, and deletes
categorized clones. The PDF oracle also uses an exact positive token, always
runs its durable delivery audit, rejects unsolicited current-only scope, and
requires an 8/10 visual result without blocking layout defects.

## 2026-09-08 — multi-domain spatial composition

See `history/2026-09-08/README.md` for the evidence-decomposition and flexible
layout correction. Composed images now retain each independently sourced
subject and support validated named or normalized regions; document authoring
uses the same compositor for text over embedded images while keeping ordinary
page regions in safe normal flow. Local rendering, VPS image/PDF inspection,
real-channel delivery, classifier contracts, and restart checks passed.
The same record documents the follow-up completion-budget correction after a
valid multi-region plan was truncated before its closing JSON delimiter.

## 2026-09-08 — bounded queue-session recovery

See `history/2026-09-08/README.md` for the production-gate failure in which a
background agent session remained active after a queue worker expected terminal
delivery. The fix fences the background task before releasing durable work,
separates nested operation timeouts from the queue deadline, and strengthens the
live concurrency test to reject semantically wrong replies.
The same gate found two additional release blockers: queued media routing
dropped the staged source arguments before the host shortcut, and document
tests accepted a PDF without proving it was readable or visually balanced.
The dated record covers the routed quoted-image edit, generic sparse-page
compaction, and render-based PDF evaluation added to prevent recurrence.

## 2026-09-07 — release queue, scheduler, and lifecycle hardening

See `history/2026-09-07/README.md` for standalone Compose secret hydration,
noninteractive post-ready knowledge synchronization, destination-safe queue
recovery, and task-local schedule delivery correlation. The release evidence
now distinguishes accepted transport delivery from generated output and proves
that simple reminders execute once with their requested content.

## 2026-09-06 — scoped notes and request control

See `history/2026-09-06/README.md` for the durable note boundary, semantic
active-turn cancellation, healthy Zalo standby behavior, and OpenBao-backed
retention defaults. The same record covers the atomic task-aware proxy rename
to Router Worker and its upgrade cleanup boundary. It also records the verified
teardown requirement: transient OpenBao values are reloaded before backup and
Compose parsing, and an incomplete OmniRoute inventory blocks destruction.
The Zalo autosender now uses a real per-turn clock and token, preventing a late
artifact from one request from leaking into or muting a later request.
The architecture record now distinguishes active-active HTTP routing from the
active-passive Zalo owner path, and Grafana provisioning has one canonical
host-mounted tree with uniqueness checks.
Outbound delivery is now a bridge-acknowledged PostgreSQL event, so HA recovery
and DM/group isolation evidence remains durable even when the bridge does not
emit self-message journal events. External fixture discovery also supports
nested release worktrees without machine-specific paths.
The Zalo owner lease has an event-loop-independent heartbeat and stale-owner
send fence, preventing synchronous model work from triggering a false takeover
or allowing a genuinely superseded owner to deliver late.
The pre-lock request-control path now reuses deterministic access and group
addressing boundaries before semantic cancellation, and its release evidence
contains no internal execution identifiers.
The same dated record follows the continuous-message production gate from its
initial timeout through the core fixes and final pass. Per-conversation SSE
sequencing and execution locks preserve FIFO without blocking an independent
DM/group stream; quote/session/delivery state is bound to the claimed source
message, and the release evaluator executes as valid nested Python.

The dated record also covers privacy-preserving provider-session correlation
and the 10-million-record memory index gate. That gate exposed and corrected an
expression-index mismatch that made full-text retrieval scan the corpus.
It additionally records the clean-deploy documentation ownership failure and
the owner-failover delivery gap. Post-ready learning now repairs only its
validated persistent mirror and propagates failure; queue recovery now retains
the terminal response, performs a fenced channel send, and acknowledges only
after delivery succeeds.

## 2026-09-05 — update isolation, routing consolidation, and Zalo HA

See `history/2026-09-05/README.md` for the update/watchdog race, transient
OpenBao secret propagation, retired routing cleanup, dependency replacement,
scheduled media plan handoff, grounded image composition, stale Zalo SSE recovery,
image-edit routing, quoted-media ownership, slow-provider resilience,
single-deliverable visual documents, and the
model-authored office-artifact release gate, and Traefik-routed Zalo ownership
with renewable Valkey failover.
